import { SearchKeyWordPipe } from './search-key-word.pipe';

describe('SearchKeyWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchKeyWordPipe();
    expect(pipe).toBeTruthy();
  });
});
