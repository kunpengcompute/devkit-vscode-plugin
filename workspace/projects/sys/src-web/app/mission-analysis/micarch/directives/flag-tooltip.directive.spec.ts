import { FlagTooltipDirective } from './flag-tooltip.directive';

describe('FlagTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
