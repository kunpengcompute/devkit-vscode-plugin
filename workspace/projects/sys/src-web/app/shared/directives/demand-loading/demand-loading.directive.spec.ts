import { DemandLoadingDirective } from './demand-loading.directive';

describe('DemandLoadingDirective', () => {
  it('should create an instance', () => {
    // const directive = new DemandLoadingDirective();
    // expect(directive).toBeTruthy();
  });
});
